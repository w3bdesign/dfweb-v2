/* globals cy */

describe(`The Home Page`, () => {
  it(`successfully loads`, () => {
    cy.visit(`/`)
   // cy.get('.nav').contains('About')
    //.waitForRouteChange()
  })
 // cy.get('.nav').contains('About')
})
